module<package> GatewayApp {
  meta {
    label zh "GatewayApp"
    summary zh "WarpGateWay 控制平台的应用层边界：应用编排、用户侧接口与前端 WEB。"
    tag "control"
    tag "application"
    tag "package"
  }

  layer Application
}
