module<interface> UserFacingInterface {
  meta {
    label zh "用户侧服务接口"
    label en "User Facing Service Interface"
    summary zh "WarpGateWay 面向运维用户和管理员的 HTTP 接口，统一承载已认证安装代码获取、Agent 状态查询、暂停和远程升级能力。"
    tag "user-facing"
    tag "admin"
    tag "web"
    tag "backend"
    tag "http"
    tag "interface"
  }

  layer Application
  owns WarpGateWayPublicInstallInterface
  owns WarpGateWayManagementInterface
  owns AdminGetAgentInstallCode
  owns AdminShowAgentRuntimeStatus
  owns AdminGetAgentOverview
  owns AdminPauseAgent
  owns AdminUpgradeAgent
  owns AdminAgentInstallCodeReturned
  owns AdminAgentRuntimeStatusReturned
  owns AdminAgentOverviewReturned
  owns AdminPauseAgentDispatchReturned
  owns AdminUpgradeAgentDispatchReturned
  provides WarpGateWayPublicInstallInterface by protocol<http>
  provides WarpGateWayManagementInterface by protocol<http>
  depends Enrollment
  depends Identity
  depends AgentdStatusManagement
  depends AgentCommandControl
  responsible_for "用户侧服务接口：作为浏览器端和管理员用户的 Application 层入口，提供已认证安装代码、状态查询、暂停和升级 API。"
}
