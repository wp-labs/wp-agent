module<logic> Agent.Registration {
  meta {
    label zh "注册与引导逻辑"
    label en "AgentEnrollment Service"
    summary zh "负责 Agent 安装代码命令与 Agentd 注册流程。"
    tags "control", "enrollment"
  }

  layer Domain
  depends Control.Agent.Enrollment
  depends Control.Agent.Identity
  depends Control.Agent.Registry
}
