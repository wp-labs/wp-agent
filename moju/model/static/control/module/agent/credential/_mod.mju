module<logic> Agent.Credential {
  meta {
    label zh "Agent 身份逻辑"
    label en "Agent AgentIdentity Service"
    summary zh "负责 Agent 凭据续期与校验命令、校验流程。"
    tags "control", "identity"
  }

  layer Domain
  depends Control.Agent.Identity
}
