// 规范命名词典：canonical term -> aliases（历史/变体拼法）
// 校验扫描标识符与 meta 文案，命中 alias 时发 warning 建议规范形。

lexicon {
  term Gateway { aliases "GateWay", "WarpGateway", "WarpGateWay" }
  term Agentd { aliases "WarpAgentd" }
  term WarpInsightCenter { aliases "WarpInsight" }
}
