module<logic> Agent.Command {
  meta {
    label zh "Agent 控制指令"
    label en "Agent Command Control"
    summary zh "负责管理员控制意图、Agent 下行指令类型和长轮询返回结果。"
    tags "control", "agentd", "command"
  }

  layer Domain
}
