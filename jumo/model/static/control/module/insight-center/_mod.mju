// Control.InsightCenter 有界上下文包：WistCenter 上级聚合与治理。
// 承载网关接入监管、实例管理、身份、面向网关/管理接口、版本发布与中心应用编排。

module<package> InsightCenter {
  meta {
    label zh "中心监管"
    label en "Insight Center"
    summary zh "WistCenter 上级聚合与治理的职责边界：网关接入监管、实例管理、身份、面向网关/管理接口、版本发布与中心应用编排。"
    tags "control", "center", "package"
  }

  layer Domain
}
