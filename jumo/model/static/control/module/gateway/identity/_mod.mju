module<entity> Gateway.Identity {
  meta {
    label zh "Gateway 身份"
    label en "Gateway Identity"
    summary zh "负责 WarpGateway 实例接入 WistCenter 后的稳定身份与身份状态实体。"
    tags "control", "gateway", "identity"
  }

  layer Domain
}
