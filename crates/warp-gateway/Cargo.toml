# @jumo generated
# @jumo hash=d2b0d37b3a089d76

[package]
name = "warp-gateway"
version = "0.1.0"
edition = "2021"

[dependencies]
#jumo-core = { path = "/Users/zuowenjian/devspace/rust/x-topology/moju-sys/jumo-core" }
jumo-derive = "0.1"
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
toml = "0.8"
base64 = "0.22"
hyper = "1"
hyper-util = { version = "0.1", features = ["server-auto", "tokio", "service", "http1", "http2"] }
rustls = { version = "0.23", default-features = false, features = ["std", "ring"] }
rustls-pki-types = "1"
rustls-webpki = "0.103"
tokio-rustls = { version = "0.26", default-features = false, features = ["ring", "tls12", "logging"] }
wist-contracts = { path = "../wist-contracts" }
insight-control = { path = "../insight-control" }
wist-reporting = { path = "../wist-reporting" }
ring = "0.17"
serde_json = "1"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
