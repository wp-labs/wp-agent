[[connectors]]
allow_override = ["brokers", "topic", "fmt", "protocol", "num_partitions", "replication", "config", "data_format", "tag"]
id = "kafka_sink"
type = "kafka"

[connectors.params]
brokers = "localhost:9092"
data_format = "arrow_ipc"
fmt = "json"
num_partitions = 1
protocol = "text"
replication = 1
topic = "wp_events"
