# Global UI — Generated Jumo Project

This directory contains a `jumo-ui-model.json` describing the UI model.

## Quick Start

```bash
cd "../../warp-insight/crates/gateway-web"
npm install
npx tsx generate.ts jumo-ui-model.json .
```
